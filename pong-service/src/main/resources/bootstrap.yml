spring:
  application:
    name: ping-service
  cloud:
    config:
      uri: http://config:10000
      fail-fast: true

---

spring:
  profiles: development
  cloud:
    config:
      uri: http://localhost:10000
      fail-fast: true

